- Clear redered contents and show error when there is error in UI mode
- Usage of parameters ${{ parameters.pool }} which is not defined in the file
- Identify error when steps: is not under jobs: (especially when the job is under a loop)
jobs:
- ${{ each cfg in parameters.testConfigurations }}:
  - job: Run_${{ replace(cfg.agentName, '-', '_') }}
    displayName: 'Run : ${{ cfg.agentName }}'
    ${{ if ne(parameters.dependsOn, '') }}:
      dependsOn: ${{ parameters.dependsOn }}
    workspace:
      clean: all
    pool:
      name: ${{ parameters.pool }}
      demands:
      - Agent.Name -equals ${{ cfg.agentName }}
   steps:
