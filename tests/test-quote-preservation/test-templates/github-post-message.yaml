# Template with QUOTED inline
parameters:
- name: message
  type: string
  default: ''


steps:
- task: PythonScript@0
  displayName: "Comment on Pull Request"
  inputs:
    scriptSource: 'inline'
    script: |
      print('${{ parameters.message }}')
