# aps-format=false
stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - template: trailing-space-runtime-var-tmpl.yaml
      parameters:
        restorePath: $(Agent.TempDirectory)\nuget\packages
        extraArgs: ' '
